
curseforge {
    if (project.hasProperty("tempCurseAPI"))
        apiKey = tempCurseAPI
    project {
        id = curseID
        releaseType = curseBuildType
        changelog = ''
        addGameVersion version_minecraft
        addArtifact sourcesJar
        relations {
            requiredLibrary 'origin'
        }
    }
}
